name: PROD - test pull request

on:
    pull_request:
        paths:
            - '**/prod-*.tfvar'

jobs:
    test_pull_request_path:
        runs-on: utunbu-latest

        steps:
            - name: print something
              run: echo "TEST - PROD"