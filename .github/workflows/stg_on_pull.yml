name: STG - test pull request

on:
    pull_request:
        paths:
            - '**/stg-*.tfvars'

jobs:
    test_pull_request_path:
        runs-on: ubuntu-latest

        steps:
            - name: print something
              run: echo "TEST - STG"