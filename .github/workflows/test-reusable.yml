name: test reusable
on: [push]

jobs:
    test:
      uses: noh4nsen/github_actions_wizardry/.github/workflows/tf-plan.yml
      with:
        module: custodian
        target: ''
        environment: DEV
        region: us-west-2